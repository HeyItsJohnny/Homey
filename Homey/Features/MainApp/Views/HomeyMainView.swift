import SwiftUI

struct HomeyMainView: View {
    var body: some View {
        HomeDashboardView()
    }
}

#Preview("Homey Main") {
    HomeyMainView()
        .previewInterfaceOrientation(.landscapeLeft)
}
